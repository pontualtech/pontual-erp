;==============================================================================
; manager.conf (AMI) — gerado em {{generatedAt}}
; DESABILITADO na F1 (usamos ARI exclusivo).
; Mantido como template pra referência. Habilitar só se algum recurso
; específico não tiver equivalente ARI.
;==============================================================================

[general]
enabled=no
bindaddr=127.0.0.1
port=5038